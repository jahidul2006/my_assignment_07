import PropTypes from 'prop-types';

const Player = ({ player }) => {
     console.log(Player);
     
     return (
          <div>
               
          </div>
     );
};

 Player.PropTypes = {
     player: PropTypes.object.isRequired
 }

export default Player;